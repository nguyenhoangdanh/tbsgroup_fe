export default function UsersLayout({children}: {children: React.ReactNode}) {
  Define breadcrumb items for users section
  Breadcrumbs will be handled by AdminLayout now
  return <div>{children}</div>;
}
